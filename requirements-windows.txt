# Windows-optimized requirements for Unsloth custom training
# Use this file if you encounter dependency conflicts with the main requirements.txt

# Unsloth - use the latest version that supports Windows
git+https://github.com/unslothai/unsloth.git

# Core PyTorch for Windows with CUDA support
# Install PyTorch separately first:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# Essential transformers ecosystem
transformers>=4.44.0
datasets>=2.21.0
accelerate>=0.33.0
bitsandbytes>=0.45.5
peft>=0.12.0
trl>=0.10.0

# Data processing
pandas>=2.2.0
numpy>=1.26.0

# Environment configuration (essential for our setup)
python-dotenv>=1.0.0

# Utilities
tqdm>=4.66.0
colorama>=0.4.6

# Additional dependencies
safetensors>=0.4.0
protobuf>=4.25.0
packaging>=24.0
typing-extensions>=4.12.0

# Optional (install only if needed)
# matplotlib>=3.9.0
# seaborn>=0.13.0
# wandb>=0.17.0
# tensorboard>=2.17.0
# rich>=13.7.0
# ninja>=1.11.0
