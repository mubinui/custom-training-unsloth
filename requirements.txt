# Windows RTX 5080 Requirements
# Install PyTorch first with: pip install --pre torch torchvision torchaudio --index-url https://download.pytorch.org/whl/nightly/cu124

# Core ML libraries
transformers>=4.44.0
datasets>=2.20.0
accelerate>=0.33.0
peft>=0.12.0
trl>=0.9.0
bitsandbytes>=0.45.5

# Data processing
pandas>=2.2.0
numpy>=1.24.0

# Environment and utilities
python-dotenv>=1.0.0
tqdm>=4.66.0
colorama>=0.4.6
rich>=13.7.0

# Additional dependencies
safetensors>=0.4.0
protobuf>=4.25.0
packaging>=24.0
typing-extensions>=4.12.0

# Unsloth (install separately after other packages)
# pip install git+https://github.com/unslothai/unsloth.git

# Note: triton and flash-attn are not compatible with Windows
# Note: xformers is optional and may cause conflicts
